{% extends 'base.html.twig' %}

{% block title %}Modifier Formateur{% endblock %}

{% block body %}
    <h1>Modifier un formateur</h1>

    {{ include('formateur/_form.html.twig', {'button_label': 'Update'}) }}

    <button class="btn-dark" type="submit">
        <a href="{{ path('formateur_index') }}">Retour à la liste</a>
    </button>


    {{ include('formateur/_delete_form.html.twig') }}
{% endblock %}
