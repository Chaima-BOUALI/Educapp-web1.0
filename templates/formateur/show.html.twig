{% extends 'base.html.twig' %}

{% block title %}Formateur{% endblock %}

{% block body %}
    <h1>Formateur</h1>

    <table class="table">
        <tbody>
<<<<<<< HEAD
        <tr>
            <th>Id</th>
            <td>{{ formateur.id }}</td>
        </tr>
        <tr>
            <th>NomFormateur</th>
            <td>{{ formateur.nomFormateur }}</td>
        </tr>
        <tr>
            <th>PrenomFormateur</th>
            <td>{{ formateur.prenomFormateur }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ formateur.email }}</td>
        </tr>
        <tr>
            <th>NumeroFormateur</th>
            <td>{{ formateur.numeroFormateur }}</td>
        </tr>
        <tr>
            <th>Sexe</th>
            <td>{{ formateur.sexe }}</td>
        </tr>
        <tr>
            <th>DateDeNaissance</th>
            <td>{{ formateur.dateDeNaissance ? formateur.dateDeNaissance|date('Y-m-d') : '' }}</td>
        </tr>
        </tbody>
    </table>
    <button type="submit">
        <a href="{{ path('formateur_index') }}">Afficher la liste des formateurs</a>
    </button>
    <button type="submit">
        <a href="{{ path('formateur_edit', {'id': formateur.id}) }}">Modifier un formateur</a>
    </button>
=======
            <tr>
                <th>Id</th>
                <td>{{ formateur.id }}</td>
            </tr>
            <tr>
                <th>NomFormateur</th>
                <td>{{ formateur.nomFormateur }}</td>
            </tr>
            <tr>
                <th>PrenomFormateur</th>
                <td>{{ formateur.prenomFormateur }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ formateur.email }}</td>
            </tr>
            <tr>
                <th>NumeroFormateur</th>
                <td>{{ formateur.numeroFormateur }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ formateur.sexe }}</td>
            </tr>
            <tr>
                <th>DateDeNaissance</th>
                <td>{{ formateur.dateDeNaissance ? formateur.dateDeNaissance|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>
<button type="submit">
    <a href="{{ path('formateur_index') }}">Afficher la liste des formateurs</a>
</button>
<button type="submit">
    <a href="{{ path('formateur_edit', {'id': formateur.id}) }}">Modifier un formateur</a>
</button>
>>>>>>> a7c45923a7d8dc09346915264934b22b0081650d
    {{ include('formateur/_delete_form.html.twig') }}
{% endblock %}
