{% extends 'base.html.twig' %}

{% block title %}Formateur{% endblock %}

{% block body %}
    <h1>Formateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formateur.id }}</td>
            </tr>
            <tr>
                <th>NomFormateur</th>
                <td>{{ formateur.nomFormateur }}</td>
            </tr>
            <tr>
                <th>PrenomFormateur</th>
                <td>{{ formateur.prenomFormateur }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ formateur.email }}</td>
            </tr>
            <tr>
                <th>NumeroFormateur</th>
                <td>{{ formateur.numeroFormateur }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ formateur.sexe }}</td>
            </tr>
            <tr>
                <th>DateDeNaissance</th>
                <td>{{ formateur.dateDeNaissance ? formateur.dateDeNaissance|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>


<button class="btn-warning">
    <a href="{{ path('formateur_index') }}">Retour à la liste</a>
</button>
<br>
<br>

<button class="btn-outline-primary">
    <a href="{{ path('formateur_edit', {'id': candidat.id}) }}">Modifier</a>
</button>
<br>
<br>
    {{ include('formateur/_delete_form.html.twig') }}
{% endblock %}